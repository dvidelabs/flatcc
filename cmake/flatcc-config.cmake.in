@PACKAGE_INIT@

set(FLATCC_RTONLY @FLATCC_RTONLY@)
set(FLATCC_REFLECTION @FLATCC_REFLECTION@)

cmake_minimum_required(VERSION 3.3)

@PACKAGE_INIT@

# Make available all imported targets
include("${CMAKE_CURRENT_LIST_DIR}/flatcc-targets.cmake")

# Make available the cmake scripts in the current folder
list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}")

find_package(FlatCCNative REQUIRED)

check_required_components(flatcc)
